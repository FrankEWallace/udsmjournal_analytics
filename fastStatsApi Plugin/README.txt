1. Copy the `fastStatsApi` folder to `plugins/generic/fastStatsApi/

2. Copy `fast-stats` folder to your OJS `api/v1/

Then login to OJS got to Settings then Plugins then Choose the fastStatsApi plugin and enable it